#!/bin/bash

# Next.js 프로덕션 서버 실행 스크립트
# 사용법: ./start.sh 또는 bash start.sh

set -e

echo "🚀 프로덕션 서버를 시작합니다..."

# 빌드 확인
if [ ! -d ".next" ]; then
  echo "⚠️  빌드가 없습니다. 먼저 빌드를 실행합니다..."
  ./build.sh
fi

# 프로덕션 서버 실행
echo "🌐 프로덕션 서버를 시작합니다. http://localhost:3000 에서 확인하세요."
npm run start

