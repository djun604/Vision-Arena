# ⚠️ Cloudflare Pages Dashboard 설정 필수

## 🔴 현재 문제

로그에서 확인:
```
No build command specified. Skipping build step.
Error: Output directory "out" not found.
```

**원인**: Cloudflare Pages Dashboard에 **Build command**가 설정되지 않았습니다.

## ✅ 해결 방법 (필수)

### 단계별 설정 가이드

1. **Cloudflare Dashboard 접속**
   - https://dash.cloudflare.com 접속
   - 로그인

2. **Pages 프로젝트로 이동**
   - 왼쪽 메뉴에서 **Workers & Pages** 클릭
   - 상단 탭에서 **Pages** 선택
   - 프로젝트 이름 클릭 (예: `vision-arena`)

3. **Settings 탭 클릭**
   - 프로젝트 페이지에서 **Settings** 탭 클릭

4. **Builds & deployments 섹션 찾기**
   - 스크롤 다운하여 **Builds & deployments** 섹션 찾기

5. **다음 설정 입력** (중요!)

   | 필드 | 값 |
   |------|-----|
   | **Framework preset** | `Next.js` 선택 |
   | **Build command** | `npm run build` 입력 ⚠️ **필수!** |
   | **Build output directory** | `out` 입력 |
   | **Root directory** | 비워두기 (또는 `/`) |
   | **Deploy command** | 비워두기 또는 `true` |

6. **Save 버튼 클릭**
   - 설정 저장

7. **자동 재배포 확인**
   - 설정 저장 후 자동으로 재배포가 시작됩니다
   - **Deployments** 탭에서 진행 상황 확인

## 📸 설정 화면 예시

```
┌─────────────────────────────────────┐
│ Builds & deployments                │
├─────────────────────────────────────┤
│ Framework preset: [Next.js ▼]        │
│ Build command: [npm run build]       │ ← 필수!
│ Build output directory: [out]       │
│ Root directory: [ ]                 │
│ Deploy command: [ ]                 │
└─────────────────────────────────────┘
```

## 🔍 확인 방법

설정 후 배포 로그에서 다음을 확인하세요:

```
Running build command: npm run build
Installing dependencies...
✓ Compiled successfully
✓ Generating static pages (22/22)
Success: Build command completed
✓ Output directory "out" found
```

## ⚠️ 중요 사항

1. **Build command는 반드시 설정해야 합니다**
   - 없으면 빌드가 실행되지 않습니다
   - `out` 디렉토리가 생성되지 않아 배포가 실패합니다

2. **wrangler.jsonc 파일만으로는 부족합니다**
   - wrangler.jsonc는 출력 디렉토리만 지정합니다
   - 빌드 명령어는 Dashboard에서 설정해야 합니다

3. **설정 후 자동 재배포**
   - 설정 저장 시 자동으로 재배포가 시작됩니다
   - 또는 새 커밋 푸시 시 자동 배포됩니다

## 🚀 다음 단계

1. Dashboard에서 위 설정 완료
2. Deployments 탭에서 배포 진행 상황 확인
3. 배포 완료 후 사이트 확인

